# Commit A — implementation results

Awaiting authorization to commit. **No commit performed yet.**

---

## `git diff --stat`

```
 rustio-core/assets/css/input.css              |  34 ++++---
 rustio-core/assets/static/css/admin.css       |   2 +-
 rustio-core/assets/static/css/rustio.css      |   4 +-
 rustio-core/assets/templates/admin/base.html  | 136 +++++++++++++-------------
 rustio-core/assets/templates/admin/login.html |   2 +-
 tailwind.config.js                            |  44 ++++++---
 6 files changed, 124 insertions(+), 98 deletions(-)
```

Per-file source-line deltas (excluding the regenerated minified `admin.css`):

| File | Added | Removed | Notes |
|---|---|---|---|
| `tailwind.config.js` | 31 | 13 | brand palette, rust→teal alias, dropped legacy teal-cyan, shadow rgba updates |
| `rustio-core/assets/css/input.css` | 20 | 14 | tokens flip + 11 component edits + .results split |
| `rustio-core/assets/templates/admin/base.html` | 69 | 67 | inline `<style>` block: tokens + 28 sub-edits + comment scrubs |
| `rustio-core/assets/templates/admin/login.html` | 1 | 1 | 1 jinja-comment scrub |
| `rustio-core/assets/static/css/rustio.css` | 2 | 2 | public-site accent token flip |
| **Source total** | **123** | **97** | net +26 |
| `rustio-core/assets/static/css/admin.css` | 1 | 1 | regenerated by `make css` (minified, 1-line file) |

**`admin.css` size delta:** 23,083 bytes → 23,513 bytes (+430). Increase comes from the new green palette utilities (`bg-green-50`, `text-green-800`, `border-green-200`) for `.message-success`, plus the new `text-brand-{600,700}`, `bg-brand-600`, `hover:text-brand-700` utilities consumed by `input.css` rules.

---

## Acceptance criteria

| Check | Status | Notes |
|---|---|---|
| `cargo check --workspace` | ✓ clean | `Finished dev profile [unoptimized + debuginfo]` |
| `cargo clippy --workspace --all-targets` | ✓ clean | no warnings |
| `cargo test --workspace --lib` (sandbox) | ✓ 406 passed / 0 failed / 46 ignored | unchanged from baseline |
| `RUSTIO_TEST_DB=1 cargo test --lib -- --ignored` (PG-gated) | ⚠ 42 passed / **4 PRE-EXISTING failures** / 0 ignored | see "PG-gated test caveat" below |
| `make css-check` | ✓ clean | `css in sync` |
| `make css` | ✓ clean | rebuilds in ~540ms, no warnings |
| Negative grep: `emerald` in source | ✓ zero | input.css + base.html + login.html + rustio.css |
| Negative grep: Tailwind emerald hexes (`#059669` etc) | ✓ zero | |
| Negative grep: rust orange hexes / rgbas | ✓ zero | input.css + base.html + login.html + rustio.css + tailwind.config.js |
| Negative grep: stray un-scrubbed rust comments | ✓ zero | excluding `RUSTIO_*`, framework name, URLs, file paths |
| Positive grep: `#0d9488` / `13 148 136` / `13,148,136` in source | ✓ 15 hits | tailwind.config.js (7), input.css (2), base.html (5), rustio.css (1) |
| Positive grep: `--brand-*` token references in base.html | ✓ 21 hits | 7 alias defs + 1 `--ring-brand` def + 13 consumer refs |

### PG-gated test caveat (NOT a regression)

The 4 PG-gated failures are in `rustio-core/src/admin/suggestions_tests.rs:*`:

```
admin::suggestions_tests::schema_driven_and_compile_time_derivations_agree_when_shapes_match
admin::suggestions_tests::schema_driven_find_rejects_crafted_urls
admin::suggestions_tests::schema_driven_suggestion_disappears_when_field_present
admin::suggestions_tests::schema_driven_suggestion_fires_for_missing_field
```

**All four tests carry the same `#[ignore]` reason:**

```rust
#[ignore = "blocked: entry_builder module not yet ported; unblocks when build_admin_entries has a real body"]
```

**Why this is pre-existing, not my regression:**

1. My `git diff --name-only` shows 6 files touched: `tailwind.config.js`, 4 CSS sources, and the regenerated `admin.css`. **Zero Rust files.** A CSS-only commit cannot affect Rust module compilation.
2. The test source itself documents the failure cause in its `#[ignore]` reason — these tests will fail the moment they're run (with `--ignored`) because `build_admin_entries` is not yet implemented.
3. The earlier `SNAPSHOT-2026-05-02.md` Section 9 missed this because that script ran `cargo test -- --list` (which only enumerates names) rather than executing `--ignored` tests. The snapshot's "all sandbox tests pass" claim was correct for sandbox, but PG-gated tests were never executed there.

**Recommendation:** record this in the commit message as a known pre-existing gap. Phase 11/a does not block on it.

---

## Browser smoke matrix (live curl against `target/release/blog`)

Boot: `RUSTIO_DEMO_MODE=1 ./target/release/blog` → port 8000. Authenticated as `admin@example.com / admin`. Postgres up; Meilisearch unreachable (graceful degrade — no impact on UI).

| Route | HTTP | Body bytes | rust-orange hexes | teal hexes |
|---|---|---|---|---|
| `/static/admin.css` | 200 | 23,513 | **0** | 6 |
| `/static/rustio.css` | 200 | 13,126 | **0** | 2 (accent + accent-soft) |
| `/admin/login` | 200 | 72,576 | **0** | 5 |
| `/admin` (dashboard) | 200 | 80,082 | **0** | 6 |
| `/admin/users` | 200 | 76,956 | **0** | 6 |
| `/admin/users/new` | 200 | 77,866 | **0** | 6 |
| `/admin/users/222` | 200 | 85,330 | **0** | 6 |
| `/admin/users/222/edit` | 200 | 82,031 | **0** | 6 |
| `/admin/groups` | 200 | 83,519 | **0** | 6 |
| `/admin/history` (audit log) | 200 | 85,291 | **0** | 6 |
| `/admin/password_change` | 200 | 76,271 | **0** | 6 |

(Routes `/error`, `/forbidden` are not directly addressable; the framework hits them via 4xx pathways. The error template was rendered when I requested `/error-page-doesnotexist`, returning a small 37-byte 404 body that bypasses the inline `<style>` — the actual error template is exercised by intentional 4xx requests in tests, which all pass.)

### Spot-check verifications on rendered HTML

These are extracted from the multi-line inline `<style>` in rendered admin pages (flattened with `tr -s`):

**Principle 1 (page-head h1 letter-spacing -0.018em):**
```
.page-head h1 { font-size: 32px; font-weight: var(--rio-weight-heading); margin: 0 0 8px; color: var(--rio-text); letter-spacing: -0.018em; line-height: 1.15; font-family: var(--rio-font-heading); }
```

**Principle 5 (.main padding 32px 32px):**
```
.main { flex: 1; padding: 32px 32px; background: var(--rio-bg); overflow-y: auto; display: flex; justify-content: center; }
```

**Principle 7 (.stat-card shadow-sm only, white bg, no ring):**
```
.stat-card { background: #ffffff; border-radius: var(--rounded-lg); padding: 14px 16px; box-shadow: var(--shadow-sm); }
```

**Principle 9 (.table-wrap no outer border):**
```
.table-wrap { background: var(--rio-bg-surface-1); border-radius: 10px; box-shadow: var(--rio-shadow-card); overflow: hidden; }
```

**Principle 11 (timeline tl-success uses --green-600, not brand teal):**
```
.timeline > li.tl-success::before { box-shadow: inset 0 0 0 2px var(--green-600); }
```

**Edit 3.1 (§1 brand tokens flipped):**
```
--rio-accent: #0d9488;
--rio-accent-bg: #f0fdfa;
--rio-accent-border: #ccfbf1;
--rio-ring: 0 0 0 3px rgba(13,148,136,0.12);
```

**Edit 3.7 (.btn-primary hover + focus-visible):**
```
.btn-primary:hover { background: #0f766e; border-color: #0f766e; }
.btn-primary:focus-visible { outline: 3px solid rgba(13,148,136,0.30); outline-offset: 2px; }
```

**Decision #7 (.results th gray-200, .results td gray-100), from compiled admin.css:**
```
.results th { ... border-color: rgb(229 231 235) ... }    ← gray-200
.results td { border-color: rgb(243 244 246) ... font-size: 14px ... }    ← gray-100, 14px
```

**Edit 2.13 (.message-success now green palette), from compiled admin.css:**
```
.message-success { border-color: rgb(187 247 208) ... background-color: rgb(240 253 244) ... color: rgb(22 101 52) ... }
```
(rgb 187 247 208 = green-200, 240 253 244 = green-50, 22 101 52 = green-800)

---

## Files changed (full list)

1. `tailwind.config.js`
2. `rustio-core/assets/css/input.css`
3. `rustio-core/assets/templates/admin/base.html`
4. `rustio-core/assets/templates/admin/login.html`
5. `rustio-core/assets/static/css/rustio.css`
6. `rustio-core/assets/static/css/admin.css` (regenerated by `make css`)

Untracked (uncommitted, separate from this commit):
- `docs/inventory/SNAPSHOT-2026-05-02.md` (pre-existing reference doc from earlier session)
- `docs/inventory/COMMIT-A-SPEC.md` (this Commit A spec)
- `docs/inventory/COMMIT-A-RESULTS.md` (this results report)
- `docs/inventory/REFINEMENT-WINDMILL-2026-05-02.md` (the principle audit doc)

The `docs/inventory/` directory and its contents are NOT included in this Commit A. They can ship in a separate follow-up commit if you want them tracked, or remain as workspace-only working notes.

---

## Commit message draft (final)

```
phase 11/a: teal brand migration + Windmill design discipline

Re-anchor RustIO admin's brand color from rust orange (#B84318) to
teal #0d9488, and apply Windmill-style typography / spacing / border
discipline across the admin UI. Pure CSS / token / palette commit;
no template logic changes.

Why touching four-plus files for "one color change":

The admin's design tokens are spread across three CSS surfaces plus
a public-site stylesheet:

  1. tailwind.config.js          palette + utility class names
  2. rustio-core/assets/css/
       input.css                  semantic tokens (--ds-color-*),
                                  Tailwind-input + survivor
                                  components
  3. rustio-core/assets/
       templates/admin/base.html  the inline <style> block (1472 LOC,
                                  the v14 component design system,
                                  --rio-* tokens + every component
                                  rule)
  4. rustio-core/assets/static/
       css/rustio.css             public-site CSS (--accent token)

A clean migration re-anchors the brand at every entry point:

  - tailwind.config.js: add brand-{50,100,600,700} palette; alias the
    legacy `rust.*` keys to brand teal so the four templates that
    still reference `text-rust` keep working (sweep deferred to a
    follow-up commit). Drop unused legacy `teal: "#338899"` to avoid
    naming collision with the new brand.
  - input.css: flip `:root --ds-color-accent` from blue placeholder
    (#3B82F6) to teal — load-bearing because <html> carries no theme
    class, so :root is the active state and drives :focus-visible
    rings + .action-checkbox accent. Same flip on .theme-brand /
    .theme-rust (kept as bw-compat alias selectors).
  - base.html: rewrite --rio-accent / --rio-accent-bg / --rio-accent-
    border / --rio-ring at the §1 token block. Rename §1.5
    --emerald-* aliases to --brand-* (single grep+replace; alias
    mechanism unchanged). Update .btn-primary's hardcoded hover
    `#9c3815` and rgba(184,67,26,*) shadow / outline colors to brand
    teal hexes / rgba.
  - rustio.css: flip --accent / --accent-soft tokens; cascades to all
    17 consumer rules without per-site edits.

Windmill design discipline applied (12 principles):

  - typography: bump page-h1 letter-spacing to -0.018em; table
    headers move from text-metal to text-gray-500; primary cells
    drop from 15px to 14px and gain gray-900 color
  - spacing: table rows py-3.5 -> py-4 (target ~64px height); .main
    horizontal padding 24px -> 32px; .perm-grid / .dashboard-models
    / .stat-strip gap 8-16px -> 24px (gap-6)
  - borders: drop outer border on .table-wrap and .results
    (principle 9); .stat-card switches from inset ring to shadow-sm
    + white bg (principle 7); split .results th/td so header divider
    is gray-200 (heavier) and body dividers stay gray-100 (Decision #7)
  - color discipline (principle 10): brand-600 stays only on
    affordances (active-sidebar 3px stripe, .btn-primary, focus rings,
    .pager .is-active, .filters .pill.is-active, .pane-list .row.is-
    active, .tabs > .is-active, .show-grid action links). Removed
    from decoration sites: .results tr:hover (-> bg-gray-50), .table
    .row-link:hover (-> underline only), .tip svg (-> gray-600),
    .avatar (-> soft gray), .perm-tile:hover, .dashboard-model:hover,
    .dashboard-recent link hover, breadcrumbs hover.
  - global anchor color: links read brand-700 (Decision #1
    conservative — admin scannability over Stripe-style
    everything-gray); hover keeps the same color and adds underline,
    no fill.
  - status palette (principle 11): .message-success flips from
    Tailwind emerald to green (`bg-green-50/text-green-800/
    border-green-200`); .timeline > li.tl-success switches from
    --emerald-600 (which was aliased to brand) to --green-600
    (semantic — success is GREEN, not brand teal).

Note for keyboard users: the :focus-visible ring on form fields
and links was rendering blue (#3B82F6) prior to this commit due to
a dormant :root accent variable. <html> carries no theme class, so
the .theme-brand / .theme-rust overrides at input.css:112-125 were
inactive, and the :root default (#3B82F6, a Phase 2 placeholder)
was driving every keyboard focus ring. After this migration the
ring renders teal (#0d9488) consistently with the rest of the
brand. No accessibility regression — measured WCAG contrast ratios
of #0d9488 against page surfaces:

  vs #ffffff (white):           3.744:1   AA non-text
  vs #f9fafb (gray-50 hover):   3.583:1   AA non-text
  vs #f3f4f6 (gray-100):        3.402:1   AA non-text
  vs #f0fdfa (brand-50):        3.590:1   AA non-text

All four ratios pass WCAG AA's 3:1 threshold for non-text UI
components.

Comments updated where they described the old rust visual (6 lines
in base.html / login.html). Two comments in input.css (lines 67,
110) describe the .theme-rust *selector* rather than the rust
visual; left untouched because the selector is preserved here as a
bw-compat alias and Phase 11/b will sweep both selector and
comments together. The framework name "RustIO" in proper-noun and
URL contexts left untouched.

Tests:
  cargo test --workspace --lib                406 passed; 0 failed; 46 ignored
  cargo clippy --workspace --all-targets      clean
  make css-check                              clean

  PG-gated (RUSTIO_TEST_DB=1 ... -- --ignored):
    42 passed; 4 pre-existing failures (admin::suggestions_tests::*
    blocked on entry_builder module not yet ported, per their
    `#[ignore]` reason). My CSS-only diff cannot affect Rust module
    compilation; verified `git diff --name-only` shows zero Rust
    files touched.

Browser smoke (curl against ./target/release/blog with
RUSTIO_DEMO_MODE=1, authenticated as admin@example.com): 11 routes
returned HTTP 200 with zero rust-orange hexes and ≥5 teal references
each (counts vary 5-6 per page based on which components render).
/static/admin.css and /static/rustio.css confirmed serving the
new palette.

Phase 11/b will sweep the four `text-rust` template references in
log_entries.html, object_history.html, confirm_delete.html to
`text-brand-700` and add the optional principle 12 stat-card icon
spans + 32px avatar circles in users_list rows.
```

---

## Awaiting authorization

Per the user's gate-pattern instructions:
1. ✓ All 5 source-file edits performed in spec order.
2. ✓ `make css` regenerated after each edit; admin.css size deltas inspected.
3. ✓ Acceptance criteria run; results table above.
4. ✓ Browser smoke matrix run; live HTTP results above.
5. **No commit performed.** Awaiting explicit authorization.
